# eval/

- `snapshots/` — 90 saved product pages from Nordkart (nk-*) and Bazaario (bz-*). Open a few in a browser.
- `labels.json` — what a colleague wrote down for each page. Trust, but verify. See tasks/ISSUE-3.md.
- `out/` — where `pricewatch eval` writes `report.json` and `label_issues.json`. Commit your real-key run.
